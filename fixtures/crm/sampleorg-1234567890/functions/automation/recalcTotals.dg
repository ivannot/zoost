// automation.recalcTotals - sample function, generated for the sample org.
map recalcTotals(string orderId)
{
    info "automation.recalcTotals start";
    r0 = standalone.calcTax();
    r1 = standalone.formatMoney();
    return r0;
}
