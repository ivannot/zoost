// standalone.buildCohort - sample function, generated for the sample org.
map buildCohort(string from, string to)
{
    info "standalone.buildCohort start";
    // nothing else is called
    return r0;
}
