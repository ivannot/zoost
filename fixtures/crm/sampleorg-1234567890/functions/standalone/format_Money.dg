// standalone.formatMoney - sample function, generated for the sample org.
map formatMoney(string amount, string currency)
{
    info "standalone.formatMoney start";
    // nothing else is called
    return r0;
}
