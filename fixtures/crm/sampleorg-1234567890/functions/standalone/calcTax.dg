// standalone.calcTax - sample function, generated for the sample org.
map calcTax(string amount, string rate)
{
    info "standalone.calcTax start";
    r0 = standalone.orgSettings();
    return r0;
}
