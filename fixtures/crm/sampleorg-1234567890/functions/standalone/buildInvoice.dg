// standalone.buildInvoice - sample function, generated for the sample org.
map buildInvoice(string orderId)
{
    info "standalone.buildInvoice start";
    r0 = standalone.calcTax();
    r1 = standalone.applyDiscount();
    r2 = standalone.formatMoney();
    r3 = standalone.log();
    return r0;
}
